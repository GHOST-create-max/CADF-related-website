{
  "devDependencies": {
    "playwright": "^1.63.0"
  },
  "dependencies": {
    "@fontsource-variable/fraunces": "^5.3.0",
    "@fontsource-variable/plus-jakarta-sans": "^5.3.0",
    "@sparticuz/chromium": "^153.0.0",
    "gsap": "^3.15.0",
    "sharp": "^0.35.4"
  },
  "name": "cadf-tooling",
  "private": true,
  "description": "Headless-browser tooling for the CADF static site (dev only)",
  "scripts": {
    "serve": "python3 -m http.server 8000 --bind 0.0.0.0 --directory \"../CADF( final)\"",
    "setup": "npm install && node setup-browser.js",
    "assets": "node build/assets.js",
    "build": "node build/build.js",
    "check": "node check.js",
    "shared": "node shared.js",
    "features": "node features.js",
    "modal": "node modal.js",
    "visual": "node visual.js",
    "contrast": "node contrast.js",
    "shots": "node shoot.js",
    "test": "node contrast.js && node check.js && node shared.js && node theme.js && node features.js && node modal.js && node motion.js && node visual.js && node audit.js",
    "audit": "node audit.js",
    "motion": "node motion.js",
    "theme": "node theme.js"
  }
}
